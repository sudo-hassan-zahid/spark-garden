services:
  spark-garden:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      SPARK_ADDR: 0.0.0.0:8080
      SPARK_DATA: /app/data/spark-garden.tsv
    restart: unless-stopped
